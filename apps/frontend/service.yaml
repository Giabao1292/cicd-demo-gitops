apiVersion: v1
kind: Service
metadata:
  name: cicd-learning-journal
  namespace: cicd-lab
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: cicd-learning-journal
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
